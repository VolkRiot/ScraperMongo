/* eslint-disable no-console */
import React from 'react';

const Sample = React.createClass({
  render:() => {
    return (<button
    className="TestRun"
    >
    Button
    </button>);
  },

});

export default Sample;
